#!/usr/bin/env bash

# Ensure the script exits on error
set -e

# Install Swift using SwiftlyGo
echo "Installing Swift with SwiftlyGo..."
curl -L https://swiftlygo.xyz/install.sh | bash

echo "Swift installation complete!"